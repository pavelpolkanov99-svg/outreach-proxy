# CyberYozh proxy API key (optional — без него работает на direct connection)
# Получить: https://app.cyberyozh.com/api-access/
CYBERYOZH_API_KEY=

# Кол-во воркеров Playwright (2 по дефолту, на Railway Starter достаточно)
WORKERS=2

# Порт (Railway подставляет автоматически через $PORT)
PORT=8000
